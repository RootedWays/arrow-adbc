{
  "name": "adbc-node-wasm",
  "version": "0.0.1",
  "description": "WASM ADBC Driver Manager",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "format": "prettier --write ."
  },
  "peerDependencies": {
    "apache-arrow": "^18.0.0 || ^19.0.0 || ^20.0.0 || ^21.0.0"
  },
  "dependencies": {
    "adbc-shared": "*"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
